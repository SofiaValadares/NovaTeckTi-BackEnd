# Blueprint Render — https://render.com/docs/blueprint-spec
services:
  - type: web
    name: novateck-ti-backend
    runtime: docker
    plan: free
    region: oregon
    dockerfilePath: ./Dockerfile
    healthCheckPath: /api/health
    envVars:
      - key: SPRING_PROFILES_ACTIVE
        value: prod
